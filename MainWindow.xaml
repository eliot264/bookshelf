<Window
    x:Class="Bookshelf.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Bookshelf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="clr-namespace:Bookshelf.Views"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <views:PublisherListingView DataContext="{Binding CurrentViewModel}" />
        <!--<views:CountryListingView DataContext="{Binding CurrentViewModel}" />-->
        <!--<views:ContinentTableView DataContext="{Binding CurrentViewModel}" />-->
    </Grid>
</Window>
